## Release your Appchain

To start the network, Appchain team needs to release a version in the project's Github repository, which includes the ChainSpec file and the custom data type with JSON format.